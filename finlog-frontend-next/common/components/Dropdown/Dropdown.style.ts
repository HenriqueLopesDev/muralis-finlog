import { SxProps, Theme } from '@mui/material'

export const MenuPaperStyles: SxProps<Theme> = {
  borderRadius: '8px',
  border: '1px solid var(--color-contrast-00)',
  boxShadow: '0px 2px 6px rgba(0, 0, 0, 0.08)',
  background: '#ffffff',
}
